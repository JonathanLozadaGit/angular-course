import { BirthdayPipePipe } from './birthday-pipe.pipe';

describe('BirthdayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BirthdayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
